{% extends 'base.html.twig' %}

{% block title %}Liste des cafés
{% endblock %}

{% block body %}
	<main class="container">
		<section class="row">
			{% for category in categories %}
				<div class="col-4 mb-3">
					<div class="card h-100">
						<div class="card-body">
							<h5 class="card-title">{{ category.name }}</h5>
							<a href="{{ path('coffee_list', {name : category.name}) }}" class="btn btn-primary">Voir les produits</a>
						</div>
					</div>
				</div>
			{% endfor %}
		</section>
	</main>
{% endblock %}
