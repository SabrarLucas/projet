{% extends "base.html.twig" %}

{% block title %}Detail
{% endblock %}

{% block body %}
	<main class="container">
		<section class="row">
			<div class="col-12">
				<table class="table">
					<thead>
						<tr>
							<th>Date commande</th>
							<th>Produit</th>
							<th>Quantités</th>
							<th>Prix</th>
						</tr>
					</thead>
					<tbody>
						{% for detail in details %}
							<td>{{ detail.orders.createdAt | date('d/m/Y') }}</td>
							<td>{{ detail.products.name }}</td>
							<td>{{ detail.quantity }}</td>
							<td>{{ detail.total }}</td>
						{% endfor %}
					</tbody>
				</table>
			</div>
		</section>
	</main>
{% endblock %}
